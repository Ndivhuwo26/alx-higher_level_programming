-- This script will list all records of a table
-- A query that will lists all records of the table second_table who have name value
SELECT score, name FROM second_table WHERE name IS NOT NULL ORDER BY score DESC; 
